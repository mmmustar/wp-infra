## Configuration WordPress
wordpressUsername: admin
wordpressPassword: "p@ssW0rdRnd!"
wordpressEmail: "admin@test.com"
wordpressBlogName: "Test Site"
wordpressSkipInstall: false

## Configuration de la base de données externe (RDS)
externalDatabase:
  host: rds-wp.cdaookoquxxr.eu-west-3.rds.amazonaws.com
  user: wp_user
  password: "StrongWpUserPass456!"
  database: wp_database
  port: 3306
  
## Désactivation de MariaDB interne
mariadb:
  enabled: false

## Configuration Ingress
ingress:
  enabled: true
  ingressClassName: nginx
  hostname: "35.180.33.10.nip.io"
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "64m"

## Configuration Service
service:
  type: ClusterIP
  port: 8080

## Configuration de la persistence
persistence:
  enabled: true
  storageClass: "local-path"
  size: 10Gi

## Configuration des sondes
livenessProbe:
  enabled: true
  initialDelaySeconds: 120
  periodSeconds: 10

readinessProbe:
  enabled: true
  initialDelaySeconds: 30
  periodSeconds: 10

## Configuration des resources
resources:
  limits:
    memory: "512Mi"
    cpu: "500m"
  requests:
    memory: "256Mi"
    cpu: "250m"