---
- name: Déployer l’infrastructure complète WordPress
  hosts: wordpress
  become: true
  gather_facts: true
  vars:
    ansible_pkg_mgr: apt
  roles:
    - common
    - docker
    - k3s
    - wordpress
